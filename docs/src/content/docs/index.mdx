---
title: python2ts + pythonlib
description: Write Python. Ship TypeScript. Run Everywhere.
template: splash
hero:
  tagline: "Two packages that bridge the Python and TypeScript worlds. Transpile Python to clean TypeScript, or use Python's best APIs directly in your TypeScript projects."
  image:
    file: ../../assets/logo.svg
  actions:
    - text: Get Started
      link: /python2ts/guide/getting-started
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/sebastian-software/python2ts
      icon: external
      variant: minimal
---

import { Card, CardGrid, Tabs, TabItem, LinkCard } from '@astrojs/starlight/components';

<div class="flex items-center justify-center gap-8 py-12 my-8">
  <img src="/python2ts/python-logo.svg" alt="Python" class="w-28 h-28 transition-all duration-300 hover:scale-110 animate-float animate-pulse-glow" />
  <div class="flex flex-col items-center gap-2 text-accent-600">
    <svg class="w-16 h-16 stroke-current animate-pulse-glow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M5 12h14M12 5l7 7-7 7"/>
    </svg>
    <span class="text-sm font-semibold">transpiles to</span>
  </div>
  <img src="/python2ts/typescript-logo.svg" alt="TypeScript" class="w-28 h-28 transition-all duration-300 hover:scale-110 animate-float-delayed animate-pulse-glow-delayed" />
</div>

<div class="flex flex-wrap justify-center gap-12 p-8 my-8 bg-gray-800 dark:bg-gray-800 rounded-xl">
  <div class="text-center">
    <div class="text-4xl font-extrabold gradient-text">2000+</div>
    <div class="text-sm text-gray-400 uppercase tracking-wider">Tests</div>
  </div>
  <div class="text-center">
    <div class="text-4xl font-extrabold gradient-text">20+</div>
    <div class="text-sm text-gray-400 uppercase tracking-wider">Modules</div>
  </div>
  <div class="text-center">
    <div class="text-4xl font-extrabold gradient-text">4</div>
    <div class="text-sm text-gray-400 uppercase tracking-wider">Runtimes</div>
  </div>
  <div class="text-center">
    <div class="text-4xl font-extrabold gradient-text">0</div>
    <div class="text-sm text-gray-400 uppercase tracking-wider">Dependencies</div>
  </div>
</div>

## Two Packages, One Mission

<CardGrid>
  <Card title="python2ts" icon="rocket">
    <img src="/python2ts/python-logo.svg" alt="" class="w-8 h-8 mb-2" />

    **The Transpiler** — Converts Python source code to clean, idiomatic TypeScript. Full type preservation, docstring conversion, and intelligent runtime imports.

    <div class="inline-flex items-center gap-3 px-4 py-3 mt-2 bg-gray-900 rounded-lg font-mono text-sm border border-gray-700">
      <span class="text-accent-600">$</span> npm install python2ts
    </div>
  </Card>
  <Card title="pythonlib" icon="open-book">
    <img src="/python2ts/typescript-logo.svg" alt="" class="w-8 h-8 mb-2" />

    **The Runtime Library** — 20+ Python standard library modules reimplemented in TypeScript. Zero dependencies, tree-shakeable, works standalone.

    <div class="inline-flex items-center gap-3 px-4 py-3 mt-2 bg-gray-900 rounded-lg font-mono text-sm border border-gray-700">
      <span class="text-accent-600">$</span> npm install pythonlib
    </div>
  </Card>
</CardGrid>

## Why python2ts?

<CardGrid stagger>
  <Card title="AI-First Workflow" icon="star">
    Python dominates AI/ML. TypeScript powers modern web apps. Bridge the gap — prototype in Python, deploy anywhere JavaScript runs.
  </Card>
  <Card title="Full Type Safety" icon="approve-check">
    Python type hints become TypeScript types automatically. Generics, Protocols, TypedDicts, Callable — all preserved.
  </Card>
  <Card title="Battle-Tested" icon="approve-check-circle">
    Comprehensive test coverage with 2000+ tests. Runs on every commit across Node.js, Bun, Deno, and browsers.
  </Card>
  <Card title="Zero Dependencies" icon="heart">
    pythonlib has zero runtime dependencies. No bloat, no supply chain risks, minimal bundle size.
  </Card>
</CardGrid>

## Code Transformation

See how Python transforms to clean TypeScript:

<div class="grid md:grid-cols-[1fr_auto_1fr] gap-4 items-center my-8 p-6 bg-gray-800 rounded-xl">
  <div class="rounded-lg overflow-hidden shadow-lg">
    <div class="bg-python px-4 py-3 text-white font-semibold text-sm flex items-center gap-2">
      <img src="/python2ts/python-logo.svg" alt="" class="w-5 h-5" />
      Python
    </div>
```python
@dataclass
class User:
    name: str
    email: str
    age: int = 0

def greet(user: User) -> str:
    return f"Hello, {user.name}!"
```
  </div>
  <div class="flex flex-col items-center gap-2 text-accent-600 py-4">
    <svg class="w-12 h-12 stroke-current animate-pulse-glow md:rotate-0 rotate-90" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M5 12h14M12 5l7 7-7 7"/>
    </svg>
    <span class="text-sm font-semibold">python2ts</span>
  </div>
  <div class="rounded-lg overflow-hidden shadow-lg">
    <div class="bg-typescript px-4 py-3 text-white font-semibold text-sm flex items-center gap-2">
      <img src="/python2ts/typescript-logo.svg" alt="" class="w-5 h-5" />
      TypeScript
    </div>
```typescript
class User {
  constructor(
    public name: string,
    public email: string,
    public age: number = 0
  ) {}
}

function greet(user: User): string {
  return `Hello, ${user.name}!`
}
```
  </div>
</div>

<Tabs>
  <TabItem label="Generics">
```python
# Python
T = TypeVar('T')

class Container(Generic[T]):
    def __init__(self, value: T) -> None:
        self.value = value

    def get(self) -> T:
        return self.value
```

```typescript
// TypeScript
class Container<T> {
  value: T

  constructor(value: T) {
    this.value = value
  }

  get(): T {
    return this.value
  }
}
```
  </TabItem>
  <TabItem label="Comprehensions">
```python
# Python
squares = [x ** 2 for x in range(10)]
evens = [x for x in nums if x % 2 == 0]
lookup = {k: v for k, v in pairs}
unique = {x for x in items}
```

```typescript
// TypeScript
let squares = range(10).map(x => pow(x, 2))
let evens = nums.filter(x => mod(x, 2) === 0)
let lookup = Object.fromEntries(pairs)
let unique = new Set(items)
```
  </TabItem>
  <TabItem label="Async/Await">
```python
# Python
async def fetch_data(url: str) -> dict:
    response = await http_get(url)
    return await response.json()

async def main():
    data = await fetch_data("https://api.example.com")
    print(data)
```

```typescript
// TypeScript
async function fetchData(url: string): Promise<object> {
  const response = await httpGet(url)
  return await response.json()
}

async function main(): Promise<void> {
  const data = await fetchData("https://api.example.com")
  console.log(data)
}
```
  </TabItem>
  <TabItem label="Pattern Matching">
```python
# Python
match command:
    case "start":
        start_server()
    case "stop":
        stop_server()
    case ["move", x, y]:
        move_to(int(x), int(y))
    case _:
        print("Unknown command")
```

```typescript
// TypeScript
if (command === "start") {
  startServer()
} else if (command === "stop") {
  stopServer()
} else if (Array.isArray(command) &&
           command[0] === "move") {
  moveTo(int(command[1]), int(command[2]))
} else {
  console.log("Unknown command")
}
```
  </TabItem>
</Tabs>

## pythonlib Modules

Use Python's best APIs in TypeScript — standalone, without the transpiler:

<CardGrid>
  <Card title="itertools" icon="list-format">
    `chain`, `combinations`, `permutations`, `product`, `cycle`, `groupby`, `takeWhile`, `dropWhile`

    ```typescript
    import { combinations } from "pythonlib/itertools"
    [...combinations([1, 2, 3], 2)]
    // [[1,2], [1,3], [2,3]]
    ```
  </Card>
  <Card title="functools" icon="setting">
    `lruCache`, `partial`, `reduce`, `pipe`, `compose`, `cache`

    ```typescript
    import { lruCache } from "pythonlib/functools"
    const fib = lruCache((n: number): number =>
      n <= 1 ? n : fib(n - 1) + fib(n - 2)
    )
    ```
  </Card>
  <Card title="collections" icon="puzzle">
    `Counter`, `defaultdict`, `deque`, `OrderedDict`

    ```typescript
    import { Counter } from "pythonlib/collections"
    new Counter("mississippi").mostCommon(2)
    // [["i", 4], ["s", 4]]
    ```
  </Card>
  <Card title="datetime" icon="seti:clock">
    `datetime`, `date`, `time`, `timedelta`

    ```typescript
    import { datetime, timedelta } from "pythonlib/datetime"
    datetime.now().add(timedelta({ days: 7 }))
    ```
  </Card>
  <Card title="pathlib" icon="document">
    `Path` with `exists`, `readText`, `writeText`, `glob`

    ```typescript
    import { Path } from "pythonlib/pathlib"
    const p = new Path("/home/user/file.txt")
    p.stem // "file"
    ```
  </Card>
  <Card title="hashlib" icon="seti:lock">
    `md5`, `sha1`, `sha256`, `sha512`, `blake2b`

    ```typescript
    import { sha256 } from "pythonlib/hashlib"
    sha256("password").hexdigest()
    ```
  </Card>
  <Card title="re" icon="magnifier">
    `search`, `match`, `findAll`, `sub`, `compile` with named groups

    ```typescript
    import { search } from "pythonlib/re"
    search("(?P<name>\\w+)@(?P<domain>\\w+)", email)
    ```
  </Card>
  <Card title="random" icon="random">
    `randInt`, `choice`, `shuffle`, `sample`, `uniform`

    ```typescript
    import { choice, shuffle } from "pythonlib/random"
    choice(["a", "b", "c"])
    ```
  </Card>
</CardGrid>

**Plus 12 more modules:** `math`, `json`, `string`, `os`, `sys`, `time`, `glob`, `shutil`, `tempfile`, `subprocess`, `urllib`, `base64`, `uuid`, `logging`, `copy`

<LinkCard title="View all modules" href="/python2ts/reference/runtime" description="Complete documentation for all 20+ pythonlib modules" />

## Runtime Support

Tested on every commit across all major JavaScript runtimes:

<div class="flex flex-wrap gap-4 justify-center my-8">
  <div class="flex items-center gap-3 px-6 py-4 bg-gray-800 rounded-lg border border-gray-700 transition-all duration-300 hover:-translate-y-1 hover:border-accent-600 hover:shadow-lg">
    <img src="https://nodejs.org/static/logos/nodejsLight.svg" alt="Node.js" class="w-8 h-8" />
    <span class="font-medium">Node.js v22, v24</span>
    <span class="text-accent-600 text-xl">✓</span>
  </div>
  <div class="flex items-center gap-3 px-6 py-4 bg-gray-800 rounded-lg border border-gray-700 transition-all duration-300 hover:-translate-y-1 hover:border-accent-600 hover:shadow-lg">
    <img src="https://bun.sh/logo.svg" alt="Bun" class="w-8 h-8" />
    <span class="font-medium">Bun Latest</span>
    <span class="text-accent-600 text-xl">✓</span>
  </div>
  <div class="flex items-center gap-3 px-6 py-4 bg-gray-800 rounded-lg border border-gray-700 transition-all duration-300 hover:-translate-y-1 hover:border-accent-600 hover:shadow-lg">
    <img src="https://deno.land/logo.svg" alt="Deno" class="w-8 h-8" />
    <span class="font-medium">Deno v2.x</span>
    <span class="text-accent-600 text-xl">✓</span>
  </div>
  <div class="flex items-center gap-3 px-6 py-4 bg-gray-800 rounded-lg border border-gray-700 transition-all duration-300 hover:-translate-y-1 hover:border-accent-600 hover:shadow-lg">
    <svg class="w-8 h-8" viewBox="0 0 24 24" fill="currentColor"><path d="M4 4h7v7H4V4zm9 0h7v7h-7V4zM4 13h7v7H4v-7zm9 0h7v7h-7v-7z"/></svg>
    <span class="font-medium">All Modern Browsers</span>
    <span class="text-accent-600 text-xl">✓</span>
  </div>
</div>

## Use Cases

<CardGrid stagger>
  <Card title="AI/ML to Production" icon="star">
    Prototype your ML algorithms in Python, then ship them as TypeScript for edge deployment, browser inference, or serverless functions.
  </Card>
  <Card title="Code Migration" icon="right-arrow">
    Gradually migrate Python codebases to TypeScript. Convert module by module while maintaining functionality.
  </Card>
  <Card title="Learning & Teaching" icon="open-book">
    Help Python developers learn TypeScript by showing side-by-side conversions. Great for tutorials and documentation.
  </Card>
  <Card title="Performance Critical" icon="rocket">
    Run Python algorithms in V8's highly optimized JavaScript engine. Benefit from JIT compilation and modern runtime features.
  </Card>
</CardGrid>

## Quick Start

<Tabs>
  <TabItem label="Transpiler">
```bash
# Install
npm install python2ts

# Transpile a file
npx python2ts input.py -o output.ts

# Or use programmatically
```

```typescript
import { transpile } from "python2ts"
const ts = transpile(pythonCode)
```
  </TabItem>
  <TabItem label="Runtime Only">
```bash
# Install just the runtime
npm install pythonlib
```

```typescript
// Use Python-style APIs directly
import { range, enumerate, zip } from "pythonlib"
import { combinations } from "pythonlib/itertools"
import { Counter } from "pythonlib/collections"
import { Path } from "pythonlib/pathlib"

for (const [i, item] of enumerate(items)) {
  console.log(i, item)
}
```
  </TabItem>
</Tabs>
